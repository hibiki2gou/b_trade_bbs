# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files.
/.env*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets

# Ignore key files for decrypting credentials and more.
/config/*.key

# Claude Code の作業用ディレクトリ。このアプリのコードではないので追跡しない。
/.claude/

# 弾・チームの画像は公式素材（著作権あり）なので git に入れない。
# ディレクトリ自体は .keep で残す（画像はローカルに置けば表示される）。
/app/assets/images/sets/*
!/app/assets/images/sets/.keep
/app/assets/images/teams/*
!/app/assets/images/teams/.keep

